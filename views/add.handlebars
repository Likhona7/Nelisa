
<h1 class="area">Add Product</h1>

<div class="row">
    <div class="col-lg-3"></div>

    <div class="col-lg-6">
        <div class="card">



<form action="/products/add" method="POST">
	<div class="">
		<label for="">Category</label>
		<select class="" name="category_id">
			{{#categories}}
				<option value="{{id}}">{{description}}</option>
			{{/categories}}
</select>
	</div>
	<div class="form-inline">
		<label for="form9">Products</label>
		<i class="fa fa-plus-square" aria-hidden="true"   style="color:lightblue;size:10px;text-shadow:2px 2px 4px #000000;"></i>
		<input type="text" name="description" id="form6" class"form-control" placeholder="add products" size="10" value="{{description}}">
	</div>

	<input class="btn btn-default-outline waves-effect glyphicon glyphicon-plus-sign" type="submit">
</form>
